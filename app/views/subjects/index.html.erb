<h1>Subject</h1>

<%= pagination %>

<ol>
<% @subjects.each do |subject| -%>
  <li value="<%= @offset + @subjects.index(subject) + 1 %>"><%= link_to subject.to_s, subject %> (<%= pluralize(number_with_delimiter(subject.articles_count), "article") %>) <%= sparkline(subject)%></li>
<% end -%>
</ol>

<%= pagination %>

<% content_for(:sidebar) do -%>
<%= raw(treelist(nil)) %>
<% end -%>